# 多星卫星执行器系统 - 开发文档
- git:
```shell
git reset --hard HEAD
git pull
```
## TODO:

### 高优先级
- [ ] **同步屏障功能实现**：`global.json`中配置了`morning_sync`和`noon_checkpoint`两个同步屏障，但当前执行流程未使用。需要在`Coordinator`中实现多星时间屏障(`TIME_BARRIER`)和状态屏障(`STATUS_BARRIER`)的同步等待逻辑
- [ ] **！！！约束评估集成**：`constraint/evaluator.h`已定义完整的约束评估接口（表达式、时间窗口、资源、信号量约束），但行为树执行时未调用。需要在`GenericExecutor`执行`Condition`节点时集成`ConstraintEvaluator`
- [ ] **真实时间窗口检验**：当前`Check_Time_Window`节点直接返回成功，需要实现真实的时间窗口检查逻辑，对比任务`window_start`/`window_end`与系统当前时间
- [ ] **信号量释放日志不完整**：终端显示S2释放`sem_ground_station_beijing`时无日志输出，检查`DistributedSemaphore::release()`的日志记录逻辑

### 中优先级
- [ ] **星间通信实际使用**：`InterSatComm`模块已初始化但未真正用于节点间消息传递，当前任务分发是本地调用。实现真正的分布式任务分发和状态同步
- [ ] **死锁检测与解决**：`global.json`配置了`deadlock_detection: true`和`deadlock_resolution: ABORT_LOWER_PRIORITY`策略，需要在`DistributedSemaphore`中实现死锁检测算法
- [ ] **任务失败重试机制**：当行为树节点执行失败时，支持基于`Decorator`节点的重试策略（如`Retry`装饰器）
- [ ] **优先级抢占支持**：`global.json`定义了`EMERGENCY`级别可抢占，需要实现高优先级任务抢占低优先级任务持有的信号量
- [ ] **更多行为树节点类型**：当前主要使用`Sequence`，需要实现`Selector`、`Parallel`、`Decorator`等节点类型的完整执行逻辑

### 低优先级
- [ ] **日志输出优化**：多线程并行执行时日志交织，考虑为每个卫星单独的日志缓冲区或结构化日志输出
- [ ] **执行状态持久化**：支持任务执行状态的快照保存与恢复，用于故障恢复场景
- [ ] **性能指标收集**：收集信号量等待时间、任务执行耗时等统计数据，用于调度优化分析
- [ ] **配置热更新**：支持在运行时动态更新部分配置（如信号量数量、节点列表）而无需重启系统

## GPT-Codex-5.3:
- 先统一目标架构（必须先决策）：是继续“单进程多星仿真”，还是切到“真分布式多进程”。当前两套并存但主路径只走本地执行，distributeAllTasks 没接入主流程，见 coordinator.cpp (line 173) 和 coordinator.cpp (line 269)。
  
- 修正行为树执行语义（P0）：BehaviorTreeParser 把组合节点 children 清空并扁平化，导致树语义失真，见 behavior_parser.cpp (line 35) 和 generic_executor.cpp (line 291)。
  
- 补齐“任务约束”数据链路（P0）：TaskSegment 未承载 constraints/resource_requirements，调度文件里的静态/动态约束基本丢失，见 types.h (line 40)、json_parser.cpp (line 192)、task_distributor.h (line 220)。
  
- 做真实时间窗与调度时钟（P0）：现在 current_time 取 planned_start，不是实时推进时钟，Check_Time_Window 实际是静态比对，见 generic_executor.cpp (line 332) 和 generic_executor.cpp (line 430)。
  
- 完善分布式信号量语义（P0）：acquire 排队直接返回失败，执行器立即判任务失败；缺少“阻塞等待/授权回调/超时清理/按task释放token”，见 distributed_semaphore.cpp (line 125) 与 generic_executor.cpp (line 538)。
  
- 打通通信闭环（P1）：processReceivedMessage 为空，消息收发缺少完整协议处理；且多处消息头字段未完整构建，见 inter_sat_comm.cpp (line 423)、distributed_semaphore.cpp (line 237)。
  
- 落地同步屏障与死锁策略（P1）：配置已解析，但运行链未使用 barriers、deadlock_detection，见 json_parser.cpp (line 375) 和 json_parser.cpp (line 395)。
  
- 修并发与可停机性（P1）：behavior_cache_ 在多线程下无锁访问，stop() 也未驱动执行循环中断，见 coordinator.h (line 70)、coordinator.cpp (line 488)、coordinator.cpp (line 227)。
  
- 工程化补齐（P2）：结构化日志、执行状态持久化、指标采集、配置热更新。